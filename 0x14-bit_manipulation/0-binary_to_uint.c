#include <stdio.h>
#include "holberton.h"

/**
 * binary_to_unit - to change binary a decimal
 **/

unsigned int binary_to_uint(const char *b)
{

	unsigned int i = 0;

	if (b == NULL)
		return (0);

	while (b)
	{
		if 
		b++;
	}
}
